# 달리기 지수 알고리즘

달리기 지수(Running Index)는 0~100점으로, 점수가 높을수록 달리기에 적합한 환경입니다.

## 점수 계산

```
점수 = 100 - (대기질 페널티 합산 + 기상 페널티 합산)
```

기상 데이터(`KMA_API_KEY`)가 없으면 대기질만으로 100점 만점 계산합니다.

---

## 가중치 배분

### 기상 + UV 데이터 있을 때 (대기질 70% + 기상 30%)

| 구분 | 항목 | 가중치 (UV 있음) | 가중치 (UV 없음) |
|---|---|---|---|
| 대기질 | 초미세먼지 (PM2.5) | 35% | 35% |
| 대기질 | 미세먼지 (PM10) | 20% | 20% |
| 대기질 | 오존 (O₃) | 15% | 15% |
| 기상 | 기온 | **10%** | 12% |
| 기상 | 습도 | **5%** | 8% |
| 기상 | 풍속 | 5% | 5% |
| 기상 | 강수 | 5% | 5% |
| 기상 | **자외선 (UV)** | **5%** | — |

> UV 데이터가 없으면 기존 가중치(기온 12%, 습도 8%)를 유지하여 하위 호환됩니다.

### 기상 데이터 없을 때 (대기질 100%)

| 항목 | 가중치 |
|---|---|
| 초미세먼지 (PM2.5) | 50% |
| 미세먼지 (PM10) | 30% |
| 오존 (O₃) | 20% |

---

## 대기질 페널티 산출 (0~100)

### 초미세먼지 (PM2.5, μg/m³)

| 구간 | 페널티 | 계산식 |
|---|---|---|
| 0 ~ 15 | 0 | 좋음 |
| 16 ~ 35 | 0 ~ 40 | `(v - 15) / 20 × 40` |
| 36 ~ 75 | 40 ~ 80 | `40 + (v - 35) / 40 × 40` |
| 76 이상 | 100 | 최대 페널티 |

### 미세먼지 (PM10, μg/m³)

| 구간 | 페널티 | 계산식 |
|---|---|---|
| 0 ~ 30 | 0 | 좋음 |
| 31 ~ 80 | 0 ~ 40 | `(v - 30) / 50 × 40` |
| 81 ~ 150 | 40 ~ 80 | `40 + (v - 80) / 70 × 40` |
| 151 이상 | 100 | 최대 페널티 |

### 오존 (O₃, ppm)

| 구간 | 페널티 | 계산식 |
|---|---|---|
| 0 ~ 0.03 | 0 | 좋음 |
| 0.031 ~ 0.09 | 0 ~ 50 | `(v - 0.03) / 0.06 × 50` |
| 0.091 이상 | 100 | 최대 페널티 |

#### 야간 감경 (20시 ~ 06시)

`getRunningIndex`에 `hour`를 전달하면 해당 시간대의 O₃ 페널티를 **30%로 감경**합니다.

```
if (hour >= 20 || hour < 7) penalty = basePenalty × 0.3
```

- **이유:** 지표면 오존은 광화학 반응(UV + NOx + VOC)으로 생성되므로 해가 진 뒤에는 생성이 멈추고,
  도심에선 NO 적정(NO + O₃ → NO₂)으로 잔여 O₃가 빠르게 소모됩니다. 실제 야간 농도가 낮 대비 매우
  낮은 상황에서 고정 임계값(0.03 ppm~)을 그대로 적용하면 센서 잔류값이나 교외 배경 농도에
  과도하게 감점되는 문제가 있어 가중치를 낮춥니다.
- `hour`를 전달하지 않으면(=`undefined`) 기존 동작과 동일하게 전 시간대 동일 페널티를 사용합니다.

---

## 기상 페널티 산출 (0~100)

### 기온 (°C) — 최적: 12~22°C

| 구간 | 페널티 | 설명 |
|---|---|---|
| 12 ~ 22 | 0 | 러닝 최적 온도 |
| 5 ~ 11 | 0 ~ 30 | 약간 쌀쌀함 |
| 23 ~ 28 | 0 ~ 30 | 약간 더움 |
| 0 ~ 4 | 30 ~ 70 | 추움 |
| 29 ~ 35 | 30 ~ 70 | 더움 |
| 0°C 미만 / 35°C 초과 | 100 | 야외 운동 부적합 |

### 습도 (%) — 최적: 40~60%

저습(건조)은 목·기도 자극이 있지만 체온 조절에는 오히려 유리하므로 감점을 낮게 잡는다.
고습은 땀 증발이 안 돼 열사병 위험이 커지므로 감점을 크게 잡는다.

| 구간 | 페널티 | 설명 |
|---|---|---|
| 40 ~ 60 | 0 | 쾌적 |
| 30 ~ 39 | 0 ~ 10 | 약간 건조 |
| 20 ~ 29 | 10 ~ 20 | 건조 |
| 20 미만 | 20 | 극건조 (상한 고정) |
| 61 ~ 80 | 0 ~ 30 | 약간 습함 |
| 81 ~ 100 | 30 ~ 100 | 고습 — 체온 조절 어려움 |

### 풍속 (m/s) — 최적: 0~3m/s

| 구간 | 페널티 | 설명 |
|---|---|---|
| 0 ~ 3 | 0 | 미풍, 쾌적 |
| 3 ~ 7 | 0 ~ 20 | 약풍, 역풍 시 약간 불편 |
| 7 ~ 10 | 20 ~ 50 | 강풍, 러닝 어려움 |
| 10 ~ 15 | 50 ~ 100 | 돌풍, 위험 |

### 강수 형태

가중치 기반 페널티와 **강제 감점**이 동시에 적용됩니다.

| 형태 | 가중치 페널티 | 강제 감점 | 실질 최대 감점 | 설명 |
|---|---|---|---|---|
| 없음 | 0 | 0 | 0 | — |
| 비 | 80 (×5% = 4) | **30** | **34** | 노면 미끄러움, 시야 저하 |
| 진눈깨비 | 90 (×5% = 4.5) | **45** | **49.5** | 비 + 눈 혼합 |
| 눈 | 100 (×5% = 5) | **50** | **55** | 야외 러닝 부적합 |

> 강제 감점은 가중치 곱셈과 별개로 점수에서 직접 차감합니다.
> 비가 오면 다른 조건이 아무리 좋아도 "자제" 이하, 눈이면 거의 확실히 "금지"로 내려갑니다.

### 자외선지수 (UV Index, 0~15+)

| 구간 | 등급 | 페널티 | 계산식 | 러닝 대응 |
|---|---|---|---|---|
| 0 ~ 2 | 낮음 | 0 | — | 자유롭게 활동 |
| 3 ~ 5 | 보통 | 0 ~ 20 | `(uv - 2) / 3 × 20` | 모자 권장 |
| 6 ~ 7 | 높음 | 20 ~ 50 | `20 + (uv - 5) / 2 × 30` | 모자 + 선글라스 |
| 8 ~ 10 | 매우높음 | 50 ~ 80 | `50 + (uv - 7) / 3 × 30` | 모자 + 선글라스 + 자외선차단제 |
| 11+ | 위험 | 100 | — | 한낮 러닝 자제 |

> 야간(20~06시)에는 UV가 물리적으로 0이므로 페널티를 0으로 고정합니다.
> UV 데이터가 없으면(`uvIndex === undefined`) 페널티를 적용하지 않고 기존 가중치를 유지합니다.
>
> 데이터 출처: 기상청 생활기상지수 조회서비스 3.0 (`getUVIdxV4`), 매일 06시/18시 발표, `UV_API_KEY` 필요.

### UV-열·습 복합 강제 감점

UV가 높은 날 기온이 20°C 이상이면 피부 온도 상승으로 체온 조절 부담이 커져 체감 더위가 기온 수치 이상으로 올라간다. 여기에 고습(>70%)이 더해지면 땀 증발이 억제돼 체온 조절이 더욱 어려워지므로 최대 1.5배까지 추가 증폭한다. 가중치 시스템과 별개로 직접 차감해 체감을 반영한다.

발동 조건: **UV ≥ 6 AND 기온 ≥ 17°C AND 주간(06~20시)**

```
uvTier     = UV 6~7 → 1 / UV 8~10 → 2 / UV 11+ → 3
tempExcess = min(기온 - 17, 15)
humFactor  = 습도 > 70% → 1 + min((습도 - 70) / 60, 0.5)  [1.0 ~ 1.5배]
           = 습도 ≤ 70% → 1.0
강제 감점  = round(uvTier × tempExcess × 2.5 × humFactor)
```

**습도 70% 이하 (humFactor = 1.0)**

| UV 등급 | 22°C | 25°C | 28°C |
|---|---|---|---|
| 높음 (6~7) | 13점 | 20점 | 28점 |
| 매우높음 (8~10) | 25점 | 40점 | 55점 |
| 위험 (11+) | 38점 | 60점 | 83점 |

**습도 85% (humFactor = 1.25)**

| UV 등급 | 22°C | 25°C | 28°C |
|---|---|---|---|
| 높음 (6~7) | 16점 | 25점 | 34점 |
| 매우높음 (8~10) | 31점 | 50점 | 69점 |
| 위험 (11+) | 47점 | 75점 | 103점 |

> 강수 강제 감점과 마찬가지로 가중치 곱셈 이후 최종 점수에서 직접 차감합니다.
> UV가 없거나(`uvIndex === undefined`) 기온 20°C 미만이면 0점입니다.
> 습도 데이터가 없으면 humFactor = 1.0 (증폭 없음)으로 처리됩니다.

---

## 복합 악화 보정

가중치 곱셈으로 인해 개별 항목의 감점이 희석되는 문제를 보완합니다.
**개별 페널티 > 25인 항목이 2개 이상** 동시에 나쁘면, 가중 합산에 추가 감점을 적용합니다.

### 계산식

```
significantPenalties = rawPenalties.filter(p > 25)
count               = significantPenalties.length

count < 2이면 → 추가 감점 없음

avgSeverity = sum(significantPenalties) / (count × 100)
compound    = count × avgSeverity × 20

최종 페널티 = 기본 가중 페널티 + compound
```

### 예시

| 항목 | 개별 페널티 | > 25? |
|---|---|---|
| PM2.5 (40 μg/m³) | 45 | O |
| PM10 (55 μg/m³) | 20 | X |
| O₃ (0.05 ppm) | 17 | X |
| 기온 (18°C) | 0 | X |
| 습도 (85%) | 48 | O |
| 강수 (없음) | 0 | X |

```
기본 가중 페널티 = 45×0.35 + 20×0.20 + 17×0.15 + 0×0.15 + 48×0.10 + 0×0.05 = 27.0
significant = [45, 48] → count = 2
avgSeverity = (45 + 48) / 200 = 0.465
compound    = 2 × 0.465 × 20 = 18.6
최종 페널티 = 27.0 + 18.6 = 45.6
점수        = 100 - 46 = 54 → '주의 필요'
```

보정 없이는 점수 73("달리기 좋음")이 되어 체감과 맞지 않는 문제를 해결합니다.

---

## 극단값 상한 (Extreme Cap)

가중치 합산만으로는 **단일 항목**이 극단적일 때 다른 값이 좋으면 '달리기 좋음'이 나올 수 있습니다.
예: PM2.5 = 100 μg/m³(매우 나쁨) 하나만 나쁘고 나머지가 양호하면 기존 계산으로 약 65점이 나와
체감과 어긋납니다. 이를 방지하기 위해 가중치 합산·강제 감점 이후 **최종 점수의 상한**을 제한합니다.

```
cap = 100
항목별 cap을 min()으로 누적 → 최종 score = min(계산된 score, cap)
```

| 항목 | 조건 | 상한 | 최대 상태 |
|---|---|---|---|
| 초미세먼지 (PM2.5) | 35~75 μg/m³ (나쁨 구간) | `55 - (v - 35) / 40 × 20` (55 → 35 선형) | caution~bad |
| 초미세먼지 (PM2.5) | > 75 μg/m³ (매우 나쁨) | 35 | bad |
| 미세먼지 (PM10) | 80~150 μg/m³ (나쁨 구간) | `55 - (v - 80) / 70 × 20` (55 → 35 선형) | caution~bad |
| 미세먼지 (PM10) | > 150 μg/m³ (매우 나쁨) | 35 | bad |
| 자외선 (UV) | ≥ 11 (위험), 주간(06~20시) | 35 | bad |
| 자외선 (UV) | 8~10 (매우높음), 주간(06~20시) | 55 | caution |
| 강수 | 눈 / 진눈깨비 | 30 | bad |
| 강수 | 비 | 45 | caution |
| 기온 | ≤ 0°C 또는 ≥ 35°C | 30 | bad |

> PM2.5·PM10은 "나쁨"과 "매우나쁨" 사이를 계단식이 아니라 **선형으로 보간**합니다.
> 예: PM10=81 → cap 55(caution), PM10=125 → cap 42(caution), PM10=139 → cap 38(bad), PM10=150 → cap 35(bad).
> 이는 "매우나쁨" 경계 직전(예: 139μg/m³)의 체감 위험을 더 정확히 반영하기 위함입니다.

- **야간 UV 예외:** `hour`가 20~06시이면 UV 상한은 적용하지 않습니다(물리적으로 0).
- **강수·기온 상한은** 기존 강제 감점과 함께 작동합니다. 강제 감점으로 이미 점수가 낮아졌다면 상한은 무의미하고, 다른 조건이 매우 좋아 점수가 높게 산출됐을 때 상한이 동작합니다.
- **기상 데이터가 없어도** PM2.5·PM10 상한은 반드시 적용됩니다. 기상청 API 타임아웃/장애 상황에서 대기질만으로 점수를 계산하는 경로에서도 `applyExtremeCap`을 거치므로, PM10 > 150일 때 절대 '달리기 좋음' 이상이 나올 수 없습니다.

---

## 야간 감점 (23시 ~ 04시)

가중치 계산과 별개로, 23시~04시(새벽)에는 최종 점수에서 **20점을 직접 차감**합니다.

```
if (hour >= 23 || hour < 4) score = max(0, score - 20)
```

- **이유:** 시야 불량, 보행자·차량과의 충돌 위험, 야간 러닝 안전 문제.
  대기질·기상이 아무리 좋아도 심야는 추천에서 밀려나야 한다.
- **영향:** 기본 80점("달리기 최적")도 60점("달리기 좋음")으로 하락.
- **메시지 오버라이드:** `야간 시간대입니다. 시야 확보가 어려워 주의가 필요합니다.`

> 강수 강제 감점(30~50점)과 야간 감점(20점)은 둘 다 적용될 수 있으며, 순서는 야간 감점 → 강수 감점 순입니다.

---

## 점수 → 상태 매핑

| 점수 | 상태 | 라벨 | 의미 |
|---|---|---|---|
| 80 ~ 100 | `great` | 달리기 최적 | 지금 바로 달리세요! |
| 60 ~ 79 | `good` | 달리기 좋음 | 좋은 환경입니다 |
| 40 ~ 59 | `caution` | 주의 필요 | 짧은 러닝 권장 |
| 20 ~ 39 | `bad` | 달리기 자제 | 실내 운동 권장 |
| 0 ~ 19 | `worst` | 달리기 금지 | 야외 활동 자제 |
| — | `unknown` | 측정 불가 | 대기질 데이터 없음 (측정소 점검 등) |

`canRun` 플래그는 점수 40 이상일 때 `true`입니다. `unknown` 상태는 항상 `false`.

---

## 결측값(측정 불가) 처리

에어코리아 측정소는 점검·통신 오류 등으로 일부 또는 전 항목을 `"-"`로 반환할 때가 있습니다.
이 값을 `0`으로 변환해 계산에 넣으면 "매우 깨끗한 공기"로 오해석되어 점수가 **100점에 가까워지는 심각한 버그**를 일으킵니다. 이를 방지하기 위한 규칙:

1. **API 응답 파싱** — `"-"`, `""`, 파싱 실패값은 `null`로 전파합니다 (`AirQualityMetrics.pm25/pm10/...: number | null`).
2. **측정소 폴백** — PM2.5 **또는** PM10이 결측이면 해당 측정소를 비정상으로 판정하고 주변 측정소로 폴백을 시도합니다(AND → OR). 이전에는 둘 다 결측일 때만 폴백 트리거였습니다.
3. **점수 계산 차단** — `getRunningIndex`에 들어온 `metrics`의 PM2.5 또는 PM10이 `null`이면 계산을 건너뛰고 `status: 'unknown'`, `score: 0`을 반환합니다. O₃만 결측이면 O₃ 페널티를 0으로 처리하고 계산을 이어갑니다.
4. **UI 표시** — `unknown` 상태는 회색 테마로 "측정 불가"를 명시하고, 대기질 칩은 숫자 대신 `—`를 표시합니다.

---

## 대기질 등급 분류 (UI 표시 기준, 6단계)

각 오염물질의 현재 수준을 사용자에게 표시할 때 아래 6단계로 분류합니다.
러닝 지수 점수 계산과는 별개이며, 대기질 상세 패널에서 사용됩니다.

### 초미세먼지 (PM2.5, μg/m³)

| 등급 | 범위 | 색상 | 설명 |
|---|---|---|---|
| 최고 | 0 ~ 8 | 진한 초록 | 러닝에 최적인 공기입니다. |
| 좋음 | 9 ~ 15 | 청록 | 야외 활동에 적합합니다. |
| 보통 | 16 ~ 25 | 파랑 | 대부분의 사람에게 무리 없는 수준입니다. |
| 민감군주의 | 26 ~ 35 | 노랑 | 민감군은 장시간 격렬한 운동을 줄이세요. |
| 나쁨 | 36 ~ 75 | 주황 | 실외 활동을 자제하고 마스크를 착용하세요. |
| 매우나쁨 | 76 이상 | 빨강 | 모든 사람이 실외 활동을 자제해야 합니다. |

### 미세먼지 (PM10, μg/m³)

| 등급 | 범위 | 색상 | 설명 |
|---|---|---|---|
| 최고 | 0 ~ 15 | 진한 초록 | 러닝에 최적인 공기입니다. |
| 좋음 | 16 ~ 30 | 청록 | 야외 활동에 적합합니다. |
| 보통 | 31 ~ 50 | 파랑 | 대부분의 사람에게 무리 없는 수준입니다. |
| 민감군주의 | 51 ~ 80 | 노랑 | 민감군은 장시간 실외 활동을 줄이세요. |
| 나쁨 | 81 ~ 150 | 주황 | 실외 활동을 자제하고 마스크를 착용하세요. |
| 매우나쁨 | 151 이상 | 빨강 | 모든 사람이 실외 활동을 자제해야 합니다. |

### 오존 (O₃, ppb)

| 등급 | 범위 | 색상 | 설명 |
|---|---|---|---|
| 최고 | 0 ~ 15 | 진한 초록 | 러닝에 최적인 공기입니다. |
| 좋음 | 16 ~ 30 | 청록 | 야외 활동에 적합합니다. |
| 보통 | 31 ~ 50 | 파랑 | 대부분의 사람에게 무리 없는 수준입니다. |
| 민감군주의 | 51 ~ 60 | 노랑 | 민감군은 장시간 격렬한 운동을 줄이세요. |
| 나쁨 | 61 ~ 90 | 주황 | 노약자·호흡기 질환자는 실외 활동을 자제하세요. |
| 매우나쁨 | 91 이상 | 빨강 | 모든 사람이 실외 활동을 자제해야 합니다. |

### 이산화질소 (NO₂, ppb)

| 등급 | 범위 | 색상 | 설명 |
|---|---|---|---|
| 최고 | 0 ~ 5 | 진한 초록 | 러닝에 최적인 공기입니다. |
| 좋음 | 6 ~ 10 | 청록 | 야외 활동에 적합합니다. |
| 보통 | 11 ~ 20 | 파랑 | 대부분의 사람에게 무리 없는 수준입니다. |
| 민감군주의 | 21 ~ 30 | 노랑 | 민감군은 오랜 노출을 피하세요. |
| 나쁨 | 31 ~ 50 | 주황 | 호흡기 질환자는 실외 활동을 자제하세요. |
| 매우나쁨 | 51 이상 | 빨강 | 모든 사람이 실외 활동을 자제해야 합니다. |

> **참고:** 환경부 공식 기준(4단계)을 러닝 활동에 맞게 6단계로 세분화한 것입니다.
> "좋음" → "최고/좋음", "보통" → "보통/민감군주의"로 나누어 러닝 중 증가하는 호흡량을 고려했습니다.

---

## 24시간 타임라인 예보

### 데이터 소스 우선순위

1. **실측값** — 에어코리아 API에서 받은 시간별 측정값
2. **과거 빈 데이터** — 현재 측정값으로 대체
3. **미래 시간** — 현재 측정값에 시간대 패턴 적용

### 시간대 패턴 (미래 예측용)

| 시간대 | 배수 | 이유 |
|---|---|---|
| 출퇴근 (7~9시, 17~19시) | ×1.4 | 차량 매연 급증 |
| 새벽 (3~6시) | ×0.6 | 교통량 감소 |
| 그 외 | ×1.0 | 기본 |

### 최적 시간 선정

24시간 예보 중 **점수 ≥ 60 (달리기 좋음 이상)** 시간대를 추려 점수 내림차순 상위 3개를 추천합니다.

---

## 데이터 출처

| 데이터 | API | 비고 |
|---|---|---|
| 대기질 (PM2.5, PM10, O₃, NO₂, CO) | 에어코리아 실시간 측정정보 | `AIR_KOREA_API_KEY` 필요 |
| 현재 기상 (기온, 습도, 풍속, 강수) | 기상청 초단기실황 | `KMA_API_KEY` 필요 (선택) |
| 시간별 기상 예보 | 기상청 단기예보 | `KMA_API_KEY` 필요 (선택) |
